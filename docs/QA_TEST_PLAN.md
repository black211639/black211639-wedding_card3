# QA_TEST_PLAN.md

## 手機測試

檢查項目：

- 390px 寬度不爆版。
- 新人姓名不換行。
- Hero 首屏文字清楚。
- 開場 overlay 按鈕可點擊。
- 資訊卡、倒數區、地圖區沒有重疊。

通過標準：主要內容可讀、可點、無水平捲動。

## 桌機測試

檢查項目：

- 版面置中且不過度拉寬。
- 照片比例正常。
- 動畫不突兀。
- 滾動段落順序正確。

通過標準：桌機觀看不破版，但仍維持 H5 電子喜帖主視覺。

## 功能測試

檢查項目：

- `intro-overlay` 可關閉。
- `music-toggle` 存在並可切換狀態。
- Hero 輪播會自動切換。
- `map-button` 可開啟 Google Maps。
- `countdown-grid` 會更新。

通過標準：Console 無主要錯誤，互動功能可正常使用。

## 圖片測試

檢查項目：

- Hero 圖片載入正常。
- Memory 圖片載入正常。
- 倒數與結尾照片載入正常。
- 圖片失敗時 placeholder 顯示正常。

通過標準：照片不拉伸，重要主體不被錯誤裁切。

## JSON 資料測試

檢查項目：

- `wedding_info.json` 是合法 JSON。
- 新郎、新娘、日期、地點、地址正確。
- `map_link` 可開啟。
- `photo_paths` 指向存在的檔案。

通過標準：頁面資料來自 JSON 並正確顯示。

## GitHub Pages 測試

檢查項目：

- 必要檔案在根目錄。
- `.nojekyll` 存在。
- Pages 設定為 `main` / `/ (root)`。
- 發布後正式網址可開啟。

通過標準：線上頁面與本機預覽主要內容一致。

## 失敗備援測試

檢查項目：

- 暫時讓 `wedding_info.json` 讀取失敗時，`embedded-wedding-data` 仍可顯示基本資料。
- 圖片路徑錯誤時，placeholder 不造成版面崩壞。
- 音樂檔不存在時，音樂按鈕不造成 JS 中斷。

通過標準：單一資源失敗不影響整站主要資訊顯示。
